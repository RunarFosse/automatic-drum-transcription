# Extract weights on best training iteration
# Test selected model on test data

# Implement new model architectures

# Find and include all datasets
# Perform training and testing over several datasets

# Optimize evaluation to run faster (switch between GPU and CPU optimally)